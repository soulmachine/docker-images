Launch a Spark standalone cluster and an Apache Zeppelin web server,

    docker-compose up


Open <http://localhost:8082> in browser to explore Apache Zeppelin UI.

Shutdown the cluster,

    docker-compose down

